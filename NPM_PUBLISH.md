# Publish to npm

Package: **`@raviraj/jenkins-mcp`**

```bash
npm whoami    # must show: raviraj
cd jenkins-mcp
npm publish --access public
```

After publish: https://www.npmjs.com/package/@raviraj/jenkins-mcp
